coverage==7.5.1
Flask==3.1.2
flask_cors==4.0.0
flasgger==0.9.7.1
flask-wtf==1.2.1
openai==1.79.0
# The unified Google SDK, replacing the deprecated google-generativeai: it
# builds a client per API key (the old one configured the key process-wide,
# which raced between concurrent requests) and compiles a Pydantic schema
# including its property order, which the direct-PNML JSON path relies on.
google-genai==2.9.0
# Schema source for the direct-PNML JSON path; both provider SDKs compile it.
# Already present transitively via openai; pinned here because we import it.
pydantic==2.13.4
prometheus_client==0.19.0
python-json-logger==0.1.11
redis==5.2.1
fakeredis==2.24.1
python-dotenv==1.2.1